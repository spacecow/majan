# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  booking:
    one: Booking
    other: Bookings

  labels:
    new: New %{o}

  simple_form:
    labels:
      booking:
        reserved_on: Date

  helpers:
    submit:
      create: Book
      booking:
      cancel: Cancel
      reserve: Reserve

  alerts:
    no_table_available: No table is available for that time interval

  errors: &errors
    messages:
      start_before_end: must start before end 
